{
  "git": {
    "commit": true,
    "tag": true,
    "push": true,
    "requireUpstream": false,
    "tagName": "${name}@${version}",
    "tagMatch": "${name}@*",
    "commitMessage": "chore(release): ${name}@${version}"
  },
  "github": {
    "release": false
  },
  "npm": {
    "publish": true,
    "ignoreVersion": false
  },
  "hooks": {
    "before:init": ["pnpm run lint", "pnpm format:check", "pnpm run typecheck"]
  },
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "conventionalcommits",
      "infile": "CHANGELOG.md",
      "gitRawCommitsOpts": {
        "path": "."
      }
    }
  }
}
